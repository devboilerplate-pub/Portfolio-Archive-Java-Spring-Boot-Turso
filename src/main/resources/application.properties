spring.application.name=portfolio-project-archive
server.port=8080
spring.thymeleaf.cache=false
spring.web.resources.cache.period=0
# Turso is configured through environment variables; no local database is used.
turso.database.url=${TURSO_DATABASE_URL:}
turso.auth.token=${TURSO_AUTH_TOKEN:}
